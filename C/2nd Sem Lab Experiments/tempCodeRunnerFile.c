
        {
            printf("a[%d][%d]=%d\n", i, j, arr[i][j]);
            printf("%d",arr[i][j];)
        }
        printf("\n");
    }
    printf("The total number of elements you have entered is:%d\n", a * b);
    for (i = 0; i < a; i++)
    {
        for (j = 0; j < b; j++)
        {
            if (arr[i][j] == 0)
                zero++;
            else
                nzero++;
        }