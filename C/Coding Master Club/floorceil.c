#include<stdio.h>
#include<math.h>
int main()
{
    double a=2.7;
    double p=ceil(a);
    double l=floor(a);
    printf("The ceil value is%lf",p);
    printf("The floor value is %lf",l);
    return 0;
}