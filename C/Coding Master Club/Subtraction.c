#include <stdio.h>
int main()
{
    int a, b;
    printf("Enter a number two find subtraction:");
    scanf("%d", &a);
    printf("Enter a number two find subtraction:");
    scanf("%d", &b);
    int c = a - b;
    printf("The subtaction of the given number is %d:", c);
    return 0;
}