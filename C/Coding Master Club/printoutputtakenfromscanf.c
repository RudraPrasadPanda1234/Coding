#include<stdio.h>
int main()
{
    int a,i,n,count;
    printf("Enter the number of inputs");
    scanf("%d",&n);
    for(i=1;i<0;i++)
    {
        printf("Enter numbers :");
        count+=scanf("%d",&a);
    }
    printf("The inputs number is:%d",count);
    return 0;
}