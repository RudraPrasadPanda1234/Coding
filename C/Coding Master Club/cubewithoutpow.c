#include <stdio.h>
int main()
{
    int a, c;
    printf("Enter a number two find the cube :");
    scanf("%d", &a);
    c = a * a * a;
    printf("The cube of the given number is : %d", c);
    return 0;
}
