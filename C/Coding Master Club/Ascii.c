#include <stdio.h>
int main()
{
    char a;
    int b;
    printf("Enter a Alphabet to find the ASCII value:");
    scanf("%c", &a);
    printf("The ASCII value of the given number is %d", a);
    return 0;
}